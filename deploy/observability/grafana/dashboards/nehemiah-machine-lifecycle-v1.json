{
  "annotations": { "list": [] },
  "editable": false,
  "graphTooltip": 1,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "nehemiah-prometheus" },
      "fieldConfig": { "defaults": { "unit": "ops" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "options": {
        "legend": { "displayMode": "table", "placement": "bottom" },
        "tooltip": { "mode": "multi" }
      },
      "targets": [
        {
          "editorMode": "code",
          "expr": "sum by (operation, result, cloud_region) (rate(nehemiah_control_machine_lifecycle_count_total[5m]))",
          "legendFormat": "control {{cloud_region}} {{operation}} {{result}}",
          "range": true,
          "refId": "A"
        },
        {
          "editorMode": "code",
          "expr": "sum by (operation, result, cloud_region) (rate(nehemiah_host_machine_operation_count_total[5m]))",
          "legendFormat": "host {{cloud_region}} {{operation}} {{result}}",
          "range": true,
          "refId": "B"
        }
      ],
      "title": "Lifecycle operation outcomes",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "nehemiah-prometheus" },
      "fieldConfig": { "defaults": { "unit": "s" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "options": {
        "legend": { "displayMode": "table", "placement": "bottom" },
        "tooltip": { "mode": "multi" }
      },
      "targets": [
        {
          "editorMode": "code",
          "expr": "histogram_quantile(0.95, sum by (le, operation, cloud_region) (rate(nehemiah_host_machine_operation_duration_seconds_bucket[5m])))",
          "legendFormat": "{{cloud_region}} {{operation}}",
          "range": true,
          "refId": "A"
        }
      ],
      "title": "Host operation p95",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "nehemiah-prometheus" },
      "fieldConfig": { "defaults": { "unit": "ops" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "id": 3,
      "options": {
        "legend": { "displayMode": "table", "placement": "bottom" },
        "tooltip": { "mode": "multi" }
      },
      "targets": [
        {
          "editorMode": "code",
          "expr": "sum by (job_name, result) (rate(nehemiah_control_job_run_count_total[5m]))",
          "legendFormat": "{{job_name}} {{result}}",
          "range": true,
          "refId": "A"
        }
      ],
      "title": "Reconciliation and lifecycle jobs",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "nehemiah-prometheus" },
      "fieldConfig": { "defaults": { "unit": "s" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "id": 4,
      "options": {
        "legend": { "displayMode": "table", "placement": "bottom" },
        "tooltip": { "mode": "multi" }
      },
      "targets": [
        {
          "editorMode": "code",
          "expr": "histogram_quantile(0.95, sum by (le, job_name) (rate(nehemiah_control_job_run_duration_seconds_bucket[5m])))",
          "legendFormat": "{{job_name}}",
          "range": true,
          "refId": "A"
        }
      ],
      "title": "Background job p95",
      "type": "timeseries"
    }
  ],
  "refresh": "30s",
  "schemaVersion": 39,
  "tags": ["nehemiah", "machine", "lifecycle", "versioned"],
  "templating": { "list": [] },
  "time": { "from": "now-6h", "to": "now" },
  "timezone": "utc",
  "title": "Nehemiah / Machine lifecycle / v1",
  "uid": "nehemiah-machine-lifecycle-v1",
  "version": 1
}
